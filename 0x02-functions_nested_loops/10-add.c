#include "main.h"
/**
 * add - add two numbers from input
 * @a: first parameter
 * @b: second parameter
 * Description: takes two numbers and add them together
 * Return: additon of two numbers
 */
int add(int a, int b)
{
	return (a + b);
}
